spring:
  application:
    name: team-arbeidsforhold-statistikk
  datasource:
    hikari:
      jdbc-url: ${PGJDBCURL}
  jpa:
    hibernate:
      ddl-auto: none
    show-sql: true
  liquibase:
    enabled: true
    change-log: classpath:db/changelog-master.yaml
