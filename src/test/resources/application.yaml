spring:
  application:
    name: team-arbeidsforhold-statistikk
  datasource:
    hikari:
      jdbc-url: jdbc:h2:mem:testdb
  jpa:
    hibernate:
      ddl-auto: none
    show-sql: true
  liquibase:
    enabled: true
    change-log: classpath:db/changelog-master.yaml
app:
  client:
    id: local-test-client
  private-key: "Ikke brukt i test, vi mocker denne i TestKeysConfig"
